version: 2


models:
  - name: stg_box_score_triple_plays
    description: |
      Individually noted triple play events from box score accounts,
      with varying degrees of detail.

    config:
      contract:
        enforced: true

    columns:
      - name: game_id
        description: "{{ doc('game_id') }}"
        data_type: varchar
      - name: defense_side
        description: ""
        data_type: varchar
      - name: fielders
        description: ""
        data_type: varchar
