version: 2


models:
  - name: event_double_plays
    description: ""


    config:
      contract:
        enforced: true

    constraints:
      - type: primary_key
        columns: &pk [event_key]

    meta:
      primary_keys: *pk

    columns:
      - name: event_key
        description: ""
        data_type: uinteger
      - name: is_double_play
        description: ""
        data_type: boolean
        constraints:
          - type: not_null
      - name: is_triple_play
        description: ""
        data_type: boolean
        constraints:
          - type: not_null
      - name: is_ground_ball_double_play
        description: ""
        data_type: boolean
        constraints:
          - type: not_null
